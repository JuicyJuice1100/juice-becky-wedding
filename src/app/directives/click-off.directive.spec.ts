import { ClickOffDirective } from './click-off.directive';

describe('ClickOffDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOffDirective();
    expect(directive).toBeTruthy();
  });
});
